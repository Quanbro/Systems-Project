%h1 Listing Users

%table
  %tr
    %th First Name
    %th Last Name
    %th Email
    %th Role

  - @users.each do |user|
    %tr
      %td= user.first_name
      %td= user.last_name
      %td= user.email
      %td= user.role
      %td= link_to 'Show', user

      - if admin
        %td= link_to 'Edit', edit_user_path(user)
        %td= link_to 'Remove', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

  - if admin      
    %td= link_to 'New User', new_user_path


