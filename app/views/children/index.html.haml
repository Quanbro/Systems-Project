%h1 Listing Children

%table
  %tr
    %th
    %th
    %th
    %th

  - @children.each do |child|
    %tr
      %td= child.first_name
      %td= child.last_name    
      %td= link_to 'Show', child
      %td= link_to 'Edit', edit_child_path(child)
      %td= link_to 'Remove', child, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Child', new_child_path
