= form_for search_path, :method => "get" do |f|
  %br
  .actions
    = f.label :filter
    = f.text_field :filter
    = f.submit 'Filter'

%h1 Programs

%table
  %tr
    %th
    %th
    %th
    %th

  - @programs.each do |program|
    %tr
      %td= program.name
      %td= link_to 'Show', program
      %td= link_to 'Edit', edit_program_path(program)
      %td= link_to 'Remove', program, :method => :delete, :data => { :confirm => 'Are you sure?' }